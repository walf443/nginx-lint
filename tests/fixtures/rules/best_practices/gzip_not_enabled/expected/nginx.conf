# Configuration with gzip enabled
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;

    server {
        listen 80;
        server_name example.com;

        location / {
            root /var/www/html;
        }
    }
}
