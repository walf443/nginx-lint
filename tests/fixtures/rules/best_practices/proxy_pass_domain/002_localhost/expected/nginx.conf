# Configuration with proxy_pass using IP address instead of localhost
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
  server_tokens off;

  server {
    listen 80;
    server_name example.com;

    location / {
      proxy_pass http://127.0.0.1:8080;
    }
  }
}
