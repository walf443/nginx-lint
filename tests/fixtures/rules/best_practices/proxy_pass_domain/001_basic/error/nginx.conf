# Configuration with proxy_pass using domain directly
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
  server_tokens off;

  server {
    listen 80;
    server_name example.com;

    location / {
      proxy_pass http://api.example.com;
    }
  }
}
