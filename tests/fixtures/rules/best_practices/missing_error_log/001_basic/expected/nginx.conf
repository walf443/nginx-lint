# Configuration with error_log
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
    gzip on;

    server {
        listen 80;
        server_name example.com;

        location / {
            root /var/www/html;
        }
    }
}
