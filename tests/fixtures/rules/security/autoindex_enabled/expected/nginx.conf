# Configuration with autoindex disabled
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
    gzip on;

    server {
        listen 80;
        server_name example.com;

        location /files {
            autoindex off;
            root /var/www/files;
        }
    }
}
