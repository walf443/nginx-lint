# Configuration with autoindex enabled (security risk)
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
    gzip on;

    server {
        listen 80;
        server_name example.com;

        location /files {
            autoindex on;
            root /var/www/files;
        }
    }
}
