# Configuration with server_tokens disabled
worker_processes auto;

http {
    # Fixed: server_tokens is off
    server_tokens off;

    server {
        listen 80;
        server_name example.com;

        location / {
            root /var/www/html;
        }
    }
}
