# Configuration with server_tokens off
worker_processes auto;

http {
    server_tokens off;

    server {
        listen 80;
        server_name example.com;

        location / {
            root /var/www/html;
        }
    }
}
