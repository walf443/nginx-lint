# Configuration with over indentation
worker_processes auto;

http {
        server_tokens off;
        gzip on;

        server {
                listen 80;
                server_name example.com;
        }
}
