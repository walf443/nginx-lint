# Configuration with wrong closing brace indentation
worker_processes auto;

http {
    server {
        listen 80;
        server_name example.com;

        location / {
            root /var/www/html;
            }
        }
    }
