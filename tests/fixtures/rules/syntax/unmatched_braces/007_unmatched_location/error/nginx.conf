# nginx configuration
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
  server_tokens on;
  gzip on;

  server {
    listen 80;
    server_name example.com;

    location /
      root /var/www/html;
      autoindex on;
    }
  }
}
