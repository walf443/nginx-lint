# Configuration with multiple server blocks
worker_processes auto;

http {
    server {
        listen 80;
        server_name example.com;
    }

    server {
        listen 443 ssl;
        server_name example.com;

        location / {
            root /var/www/html;
        # Missing closing brace for location
    # Missing closing brace for second server
}
