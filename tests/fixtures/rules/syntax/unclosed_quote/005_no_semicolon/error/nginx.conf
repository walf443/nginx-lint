# Configuration with unclosed quote in add_header
worker_processes auto;

http {
    server {
        listen 80;

        location / {
            add_header X-Custom "value;
            return 200 "ok";
        }
    }
}
