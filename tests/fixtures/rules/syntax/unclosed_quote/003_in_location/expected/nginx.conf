# Configuration with unclosed quote in rewrite
worker_processes auto;

http {
    server {
        listen 80;
        server_name example.com;

        location /old {
            rewrite ^/old/(.*)$ "/new/$1" permanent;
        }
    }
}
