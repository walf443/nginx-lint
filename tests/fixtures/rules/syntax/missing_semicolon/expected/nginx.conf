# Configuration with semicolons fixed
worker_processes auto;

http {
    server {
        listen 80;
        server_name example.com;

        location / {
            root /var/www/html;
        }
    }
}
