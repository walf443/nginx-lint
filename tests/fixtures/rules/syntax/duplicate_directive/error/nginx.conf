# Configuration with duplicate directives
worker_processes auto;
worker_processes 4;
error_log /var/log/nginx/error.log;

http {
    gzip on;

    server {
        listen 80;
        listen 80;
        server_name example.com;
        server_name example.org;

        location / {
            root /var/www/html;
            root /var/www/other;
        }
    }
}
