# Configuration with duplicate directive
worker_processes auto;
worker_processes 4;
error_log /var/log/nginx/error.log;

http {
    gzip on;

    server {
        listen 80;
        server_name example.com;

        location / {
            root /var/www/html;
        }
    }
}
