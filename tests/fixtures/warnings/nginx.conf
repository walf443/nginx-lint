# Configuration with warnings
worker_processes auto;

http {
    # Warning: server_tokens should be off
    server_tokens on;

    server {
        listen 80;
        server_name example.com;

        location / {
            root /var/www/html;
        }
    }
}
