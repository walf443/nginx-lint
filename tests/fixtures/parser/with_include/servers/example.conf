# Example.com server
server {
    listen 80;
    server_name example.com www.example.com;

    location / {
        root /var/www/example;
        index index.html;
    }

    location /api {
        proxy_pass http://localhost:3000;
    }
}
