The following third-party crates are compiled into this binary, grouped by license.
{{#each licenses}}

========================================================================
{{name}}
Used by:{{#each used_by}}{{#unless (or (eq crate.name "nginx-lint-common") (eq crate.name "nginx-lint-parser") (eq crate.name "nginx-lint-plugin"))}} {{crate.name}}{{/unless}}{{/each}}
========================================================================

{{{text}}}
{{/each}}
