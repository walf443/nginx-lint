[package]
name = "duplicate-directive-plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
nginx-lint = { path = "../../../..", features = ["plugin-sdk"] }
serde_json = "1.0"
