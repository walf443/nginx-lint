# Configuration with proxy_pass using localhost
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
  server_tokens off;

  server {
    listen 80;
    server_name example.com;

    location / {
      proxy_pass http://localhost:8080;
    }
  }
}
