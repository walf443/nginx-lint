http {
  # Good: upstream server with domain, 'resolve' parameter, and 'zone' directive
  upstream backend {
    zone backend_zone 64k;
    server api.example.com:80 resolve;
  }
  server {
    location / {
      proxy_pass http://backend;
    }
  }
}
