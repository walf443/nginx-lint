http {
  # Bad: upstream server with domain but no 'resolve' parameter
  upstream backend {
    server api.example.com:80;
  }
  server {
    location / {
      proxy_pass http://backend;
    }
  }
}
