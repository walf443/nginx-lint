# Configuration with client_max_body_size set
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
  client_max_body_size 10m;

  server {
    listen 80;
    server_name example.com;

    location / {
      root /var/www/html;
    }
  }
}
