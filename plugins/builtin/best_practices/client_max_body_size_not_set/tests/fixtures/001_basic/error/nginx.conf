# Configuration without client_max_body_size set
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
  server {
    listen 80;
    server_name example.com;

    location / {
      root /var/www/html;
    }
  }
}
