# Configuration with http-level proxy_set_header inheritance issue
http {
  # Headers set at http level
  proxy_set_header Host $host;

  server {
    listen 80;

    location / {
      # Missing http-level headers
      proxy_set_header X-Custom "value";
      proxy_pass http://backend;
    }
  }
}
