http {
    server {
        listen 80;
        root /var/www/html;

        location / {
            try_files $uri $uri/ =404;
        }

        location /api {
            alias /var/www/api/;
            proxy_pass http://backend;
        }
    }
}
