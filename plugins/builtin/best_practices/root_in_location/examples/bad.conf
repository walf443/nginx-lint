http {
    server {
        listen 80;

        location / {
            root /var/www/html;
            try_files $uri $uri/ =404;
        }

        location /api {
            root /var/www/api;
            proxy_pass http://backend;
        }
    }
}
