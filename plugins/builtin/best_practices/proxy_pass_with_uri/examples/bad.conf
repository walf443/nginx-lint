http {
    server {
        listen 80;

        # Trailing slash causes URI rewriting
        location /api/ {
            proxy_pass http://backend/;
        }

        # Path causes URI rewriting
        location /web/ {
            proxy_pass http://frontend/app/;
        }
    }
}
