# Configuration with error_page multi-code inheritance issue
http {
  server {
    listen 80;

    error_page 500 502 503 504 /50x.html;

    location / {
      # Only handles 404 - parent's 5xx handling is lost
      error_page 404 /404.html;
      root /var/www/html;
    }
  }
}
