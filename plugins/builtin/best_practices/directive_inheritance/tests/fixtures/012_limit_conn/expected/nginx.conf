# Configuration with limit_conn inheritance issue
http {
  server {
    listen 80;

    limit_conn addr 10;

    location / {
      # Explicitly repeat parent's addr limit and add perserver limit
      limit_conn addr 10;
      limit_conn perserver 100;
      root /var/www/html;
    }
  }
}
