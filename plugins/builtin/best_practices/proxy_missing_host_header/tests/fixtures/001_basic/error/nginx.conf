# Configuration with proxy_pass but missing Host header
worker_processes auto;
error_log /var/log/nginx/error.log;

http {
  server {
    listen 80;
    server_name example.com;

    location / {
      proxy_pass http://backend;
    }
  }
}
