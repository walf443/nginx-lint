http {
    server {
        listen 80;

        # location ends with / but alias doesn't - BAD
        location /images/ {
            alias /data/images;
        }

        location /docs/ {
            alias "/var/www/docs";
        }
    }
}
