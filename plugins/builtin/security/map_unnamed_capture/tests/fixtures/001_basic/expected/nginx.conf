http {
    map $host $backend {
        volatile;
        default                          default_backend;
        ~^(?<sub>.+)\.example\.com$      ${sub}_backend;
    }

    server {
        listen 80;

        location ~ ^/api/(?<rest>.*)$ {
            proxy_pass http://$backend/$rest;
        }
    }
}
