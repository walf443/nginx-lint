http {
    map $host $backend {
        volatile;
        default                 default_backend;
        ~^(.+)\.example\.com$   $1_backend;
    }

    server {
        listen 80;

        location ~ ^/api/(.*)$ {
            proxy_pass http://$backend/$1;
        }
    }
}
