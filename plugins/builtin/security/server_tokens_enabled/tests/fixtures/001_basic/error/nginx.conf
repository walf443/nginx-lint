# Configuration with server_tokens setting
worker_processes auto;

http {
  server_tokens on;

  server {
    listen 80;
    server_name example.com;

    location / {
      root /var/www/html;
    }
  }
}
