[package]
name = "deprecated-ssl-protocol-plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
nginx-lint = { path = "../../../..", default-features = false, features = ["plugin-sdk"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
